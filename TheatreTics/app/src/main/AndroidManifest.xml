<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.second.theatretics">
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AddMovieList"></activity>
        <activity android:name=".AddHallList"></activity>
        <activity android:name=".HallListActivity"></activity>
        <activity android:name=".HallBookingActivity"></activity>
        <activity android:name=".LoginwinActivity"></activity>
        <activity android:name=".CreateIdActivity"></activity>
        <activity android:name=".ProfileActivity"></activity>
        <activity android:name=".OrderActivity" />
        <activity android:name=".PaymentActivity" />
        <activity android:name=".AdminActivity" />
    </application>

</manifest>